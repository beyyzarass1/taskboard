gün 7

Görevleri `tasks` dizisinde sakladım.

`renderTasks()` ile görev listesini dinamik olarak oluşturdum.

Dashboard sayaçlarını güncelledim.
Tümü, Açık ve Tamamlanan filtrelerini ekledim.

Tamamla butonu ile görev durumunu değiştirdim.

